import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#E8F018',
    alignItems: 'center',
    justifyContent: 'space-around',
  },
  text: {
    fontSize: 20,
    color: '#000',
  },
});

export default styles;
