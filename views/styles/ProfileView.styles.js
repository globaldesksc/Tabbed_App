import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#DB7C04',
    alignItems: 'center',
    justifyContent: 'space-around',
  },
  text: {
    fontSize: 20,
    color: '#fff',
  },
});

export default styles;
